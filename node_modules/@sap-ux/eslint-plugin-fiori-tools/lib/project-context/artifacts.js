"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getProjectArtifacts = getProjectArtifacts;
const synckit_1 = require("synckit");
const project_access_1 = require("@sap-ux/project-access");
/**
 * Get Fiori project artifacts for the given file path.
 *
 * @param filePath - file path to find the project artifacts for
 * @returns Found Fiori artifacts
 */
async function getProjectArtifacts(filePath) {
    try {
        const projectRoot = await (0, project_access_1.findProjectRoot)(filePath, false);
        const projectType = await (0, project_access_1.getProjectType)(projectRoot);
        const artifacts = await (0, project_access_1.findFioriArtifacts)({
            wsFolders: [projectRoot],
            artifacts: ['applications', 'adaptations']
        });
        return { artifacts, projectType };
    }
    catch {
        return { artifacts: {}, projectType: 'EDMXBackend' };
    }
}
(0, synckit_1.runAsWorker)(getProjectArtifacts);
//# sourceMappingURL=artifacts.js.map